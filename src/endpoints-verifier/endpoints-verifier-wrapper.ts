/**
 * We can't have endpoints-verifier.ts file to run just endpoints verifier tests. That is why this file exists.
 * Read about endpoints-verifier.ts to understand why
 */

import { automationTests } from "./index";

automationTests();